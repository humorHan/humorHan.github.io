const e="### flex 的三个核心属性 flex-grow /flex-shrink/flex-basis 分别代表什么？\n\n答：这三个是给 **flex 子项** 设置的核心属性，共同决定了 flex 子项的空间分配、压缩规则，也是 flex 缩写属性的核心组成，三者均为非负数值，含义分别是：\n\n1. **flex-grow（放大因子）**：取值 ≥0，默认值 `0`。作用是：当 flex 父容器存在**剩余空间**时，子项瓜分剩余空间的「权重占比」。值越大，分到的剩余空间越多；取值为 0 时，不参与剩余空间的分配。\n2. **flex-shrink（缩小因子）**：取值 ≥0，默认值 `1`。作用是：当 flex 父容器空间**不足、子项总宽度超出父容器**时，子项被压缩的「权重占比」。值越大，被压缩的尺寸越多；取值为 0 时，该子项**不会被压缩**，宽度固定。\n3. **flex-basis（基准宽度）**：默认值 `auto`，支持 px/%/0 等值。作用是：子项在「分配剩余空间 / 被压缩」之前的**初始默认宽度**，优先级高于子项自身的 `width` 属性。取值为 0 时，子项初始宽度为 0；取值为 auto 时，子项初始宽度由自身内容宽度决定。\n\n> 核心考点：`flex:1` 就是 `flex:1 1 0%`、`flex:auto` 就是 `flex:1 1 auto`，所有差异均来源于这三个属性的取值不同。\n\n---";export{e as default};
