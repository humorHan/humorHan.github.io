const n="### 11. 前端代码分割（Code Splitting）的实现方式有哪些？（路由懒加载、动态 import、chunk 分割）\n\n**答：**代码分割是将构建产物拆分为多个小 chunk，按需加载，核心目标是减小首屏加载体积、提升加载速度，核心实现方式如下：\n\n#### 1. 路由懒加载（最常用）\n\n基于路由维度拆分代码，仅加载当前路由对应的组件，Vue/React 生态通用：\n\n```javascript\n\n```\n\n// Vue Router 示例 const Home = () => import(/_ webpackChunkName: \"home\" _/ '@/views/Home.vue')const About = () => import(/_ webpackChunkName: \"about\" _/ '@/views/About.vue')const routes = [{ path: '/', component: Home },{ path: '/about', component: About }]\n\n````\n	#### 2. 动态 import （通用方式）\n	手动拆分非路由组件 / 工具库，按需加载：\n	```javascript\n// 按钮点击后加载富文本编辑器const loadEditor = async () => {const { default: Editor } = await import(/* webpackChunkName: \"editor\" */ '@/components/Editor.vue')// 使用 Editor 组件}\n````\n\n````\n#### 3. Webpack 手动配置 chunk 分割\n通过 `splitChunks` 拆分第三方库、公共代码：\n```javascript\n````\n\n// webpack.config.js\nmodule.exports = {optimization: {splitChunks: {chunks: 'all', // 拆分同步/异步 chunkcacheGroups: {vendor: { // 拆分第三方库（如 react/vue）test: /[\\\\/]node_modules[\\\\/]/,name: 'vendors',priority: -10},common: { // 拆分公共组件/工具函数 minSize: 0, // 最小体积 minChunks: 2, // 被引用至少 2 次 name: 'common',priority: -20}}}}}\n\n````\n	#### 4. Vite 自动 / 手动分割\n	Vite 基于 Rollup 实现，可通过 `build.rollupOptions` 配置：\n	javascript\n	运行\n	```javascript\n// vite.config.tsexport default defineConfig({build: {rollupOptions: {output: {manualChunks: {// 拆分第三方库vue: ['vue', 'vue-router', 'pinia'],utils: ['lodash-es', 'axios']}}}}})\n````\n\n````";export{n as default};
