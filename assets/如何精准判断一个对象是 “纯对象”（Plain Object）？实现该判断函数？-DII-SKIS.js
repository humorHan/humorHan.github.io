const t="### 如何精准判断一个对象是 “纯对象”（Plain Object）？实现该判断函数？\n\n**答：**\n\n#### （1）纯对象（Plain Object）定义\n\n纯对象是「由 `Object` 构造函数创建」或「对象字面量 `{}` 创建」的对象，满足：\n\n- 类型是 `object` 且不是 `null`；\n- 原型链指向 `Object.prototype`（或 `null`）；\n- 不是宿主对象（如 `window`、`DOM` 元素）；\n- 不是特殊对象（如 `Array`、`Map`、`Set`、`Date` 等）。\n\n#### （2）判断函数实现\n\njavascript\n\n运行\n\n```javascript\nfunction isPlainObject(obj) {// 第一步：排除非对象类型（null/undefined/字符串/数字等）if (typeof obj !== 'object' || obj === null) {return false;}// 第二步：获取原型，处理 Object.create(null)（原型为 null）let proto = Object.getPrototypeOf(obj);if (proto === null) {return true;}// 第三步：验证原型链是否指向 Object.prototypeconst baseProto = Object.getPrototypeOf(Object.prototype);while (Object.getPrototypeOf(proto) !== baseProto) {\n    proto = Object.getPrototypeOf(proto);// 防止原型链过长（如循环原型）if (proto === null) break;}return Object.getPrototypeOf(proto) === baseProto && proto === Object.prototype;}// 测试用例console.log(isPlainObject({})); // trueconsole.log(isPlainObject(Object.create(null))); // trueconsole.log(isPlainObject(new Object())); // trueconsole.log(isPlainObject([])); // falseconsole.log(isPlainObject(new Date())); // falseconsole.log(isPlainObject(window)); // falseconsole.log(isPlainObject(null)); // false\n```";export{t as default};
