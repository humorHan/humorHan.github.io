const _='### 移动端点击事件有 300ms 延迟，是什么原因导致的？有哪些解决方案？（高频 TOP1，必问）\n\n答：\n\n1. 延迟原因：早期移动端为了判断用户是「单击」还是「双击缩放」，浏览器会等待 300ms 确认用户没有二次点击，从而出现点击延迟，会导致页面交互卡顿、点击反馈慢。\n2. 解决方案（按优先级 / 实用性排序，答全加分）：\n\n   - 方案 1：全局禁用缩放，在 html 的 meta 标签中添加配置，最常用、最简单，项目首选：`<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">`\n   - 方案 2：使用 `touchstart` 替代 `click` 事件，触发时机更早，无延迟；注意：需处理触摸穿透问题。\n   - 方案 3：引入第三方库 `fastclick.js`，自动解决移动端 300ms 延迟和触摸穿透，兼容性极佳，老项目首选。\n   - 方案 4：使用 CSS 属性 `touch-action: manipulation`，告诉浏览器只允许点击和滚动，禁用双击缩放，原生解决延迟，现代浏览器推荐。';export{_ as default};
