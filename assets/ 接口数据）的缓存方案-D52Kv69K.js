const n=`#### 如何设计合理的前端缓存策略？举例说明不同场景（静态资源 / 接口数据）的缓存方案

【核心考察点】缓存策略的落地能力，不同场景下的缓存设计思路【参考答案】

##### 一、静态资源缓存（JS/CSS/ 图片）

核心：\`文件名哈希 + 强缓存 + 协商缓存兜底\`

- 步骤 1：构建工具生成资源哈希（如 \`app.8f7d2.js\`），内容不变哈希不变；
- 步骤 2：响应头配置 \`Cache-Control: public, max-age=31536000\`（1 年强缓存）；
- 步骤 3：配置 ETag/Last-Modified，手动刷新时触发协商缓存；
- 更新策略：资源修改后哈希变化，页面引用新路径，浏览器请求新资源。

##### 二、接口数据缓存
`;export{n as default};
