const _="#### 请实现一个类型工具 `PickByType<T, U>`，从类型 `T` 中筛选出属性值类型为 `U` 的所有属性。\n\n**答案**：\n\n```typescript\ntype PickByType<T, U> = {// 遍历 T 的所有属性 K，仅保留 T[K] 等于 U 的属性[K in keyof T as T[K] extends U ? K : never]: T[K];};// 测试type User = {\n  name: string;\n  age: number;\n  isAdmin: boolean;\n  createdAt: Date;};// 筛选出 User 中类型为 string 的属性type StringProps = PickByType<User, string>; \n// StringProps 类型：{ name: string }// 筛选出类型为 number 的属性type NumberProps = PickByType<User, number>;// NumberProps 类型：{ age: number }\n```";export{_ as default};
