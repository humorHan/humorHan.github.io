const _=`### 前端中常见的「内存泄漏」场景有哪些？内存泄漏的危害？如何排查和解决？（高频 ）

答：

#### 一、内存泄漏的定义 + 危害

内存泄漏：指 JS 中**不再使用的变量 / 对象**，没有被垃圾回收机制回收，依然占用内存空间，导致内存占用率持续升高；危害：页面卡顿、操作响应变慢、浏览器崩溃、移动端 APP 闪退，严重影响用户体验。

#### 二、前端常见的 4 类内存泄漏场景（必答，高频）

1. 意外的全局变量：未声明的变量会挂载到 window 上，全局变量不会被垃圾回收，比如 \`function fn() { a = 10; }\`；

2. 未清除的定时器 / 延时器：\`setInterval/setTimeout\` 执行后，未调用 \`clearInterval/clearTimeout\`，即使页面销毁，定时器依然存在；

3. 未解绑的事件监听：比如 \`addEventListener('click', fn)\`，组件销毁 / 页面跳转时，未执行 \`removeEventListener\`，事件监听一直存在；

4. 闭包滥用：闭包会保留对外部变量的引用，导致变量无法被回收；还有：DOM 元素被删除后，JS 中仍保留该 DOM 的引用。

#### 三、排查 + 解决方案

1. 排查方式：浏览器 F12 → Memory 面板，录制内存快照，查看内存占用趋势；Performance 面板，分析内存是否持续上涨；

2. 解决方案：① 避免意外全局变量，变量声明加 \`let/const\`；② 定时器 / 事件监听，在组件销毁 / 页面卸载时**及时清除**；③ 合理使用闭包，避免无用闭包；④ DOM 元素删除后，及时清空对应的 JS 引用。`;export{_ as default};
