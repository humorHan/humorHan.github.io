const e="### 9. 为什么 Vue2 中直接修改数组下标（如 arr [0] = 1）或新增对象属性（如 obj.newKey = 2）无法触发响应式更新？如何解决？\n\n**答：**\n\n- **原因**：\n\n  1. 数组：Vue2 对数组的响应式处理是**重写了数组的 7 个变异方法**（push/pop/shift/unshift/splice/sort/reverse），但未对数组下标和 length 属性做 `Object.defineProperty()` 劫持，因此直接修改下标无法触发 setter；\n  2. 对象：Vue2 初始化时仅对已有属性做 `Object.defineProperty()` 劫持，新增属性未被劫持，因此无法触发响应式。\n- **解决方法**：\n\n  1. 数组修改：\n     - 使用 Vue 重写的变异方法（如 `arr.splice(0, 1, 1)`）；\n     - 使用 `Vue.set(arr, 0, 1)` 或 `this.$set(arr, 0, 1)`。\n  2. 对象新增属性：\n     - 使用 `Vue.set(obj, 'newKey', 2)` 或 `this.$set(obj, 'newKey', 2)`；\n     - 替换整个对象（如 `this.obj = { ...this.obj, newKey: 2 }`）。";export{e as default};
