const e="### Flex 布局的容器属性 / 项目属性各列举 5 个？核心应用场景？\n\n**答案**：Flex 布局（弹性布局）分为容器（父元素）和项目（子元素），核心是通过容器属性控制项目排列。\n\n#### 容器属性（5 个核心）：\n\n1. `flex-direction`：项目排列方向（`row`/`row-reverse`/`column`/`column-reverse`）；\n2. `justify-content`：项目在主轴上的对齐方式（`flex-start`/`center`/`flex-end`/`space-between`/`space-around`/`space-evenly`）；\n3. `align-items`：项目在交叉轴上的对齐方式（`flex-start`/`center`/`flex-end`/`baseline`/`stretch`）；\n4. `flex-wrap`：项目是否换行（`nowrap`/`wrap`/`wrap-reverse`）；\n5. `align-content`：多行项目在交叉轴上的对齐方式（仅换行时生效，`flex-start`/`center`/`flex-end`/`space-between`/`space-around`/`stretch`）。\n\n#### 项目属性（5 个核心）：\n\n1. `flex-grow`：项目的放大比例（默认 0，剩余空间不分配）；\n2. `flex-shrink`：项目的缩小比例（默认 1，空间不足时缩小）；\n3. `flex-basis`：项目的基准宽度（默认 auto，优先级高于 width）；\n4. `flex`：简写（`flex-grow flex-shrink flex-basis`，如 `flex: 1` = `1 1 0%`）；\n5. `align-self`：单个项目在交叉轴上的对齐方式（覆盖容器的 `align-items`）。\n\n#### 核心应用场景：\n\n- 水平垂直居中（容器设置 `justify-content: center; align-items: center`）；\n- 等分布局（项目设置 `flex: 1`）；\n- 导航栏布局（左对齐 logo，右对齐菜单）；\n- 响应式卡片布局（自动换行，适配不同屏幕）；\n- 移动端适配（替代浮动 / 定位，简化布局）。";export{e as default};
