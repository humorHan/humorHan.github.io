const _=`### Webpack 和 Vite 的核心差异是什么？各自的适用场景如何？Vite 的依赖预构建原理是什么？

**答：**

#### 核心差异

#### 适用场景

- **Webpack**：中大型传统项目、需兼容多模块规范 / 旧浏览器、依赖复杂的项目（如微前端、多页面应用）；
- **Vite**：Vue3/React 现代项目、追求开发体验的中小型项目、对热更新速度要求高的场景。

#### Vite 依赖预构建原理

1. **触发时机**：启动服务时自动执行，或依赖变动时重新执行；
2. **核心目标**：
   - 将 CommonJS/UMD 规范的依赖转为 ESM 规范，适配浏览器原生 ESM；
   - 将多个零散的依赖模块打包为单个文件，减少 HTTP 请求数；

3. **执行流程**：
   - 扫描项目 \`package.json\` 中的依赖，筛选出需要预构建的包；
   - 通过 Esbuild（极速 Go 语言编译器）将依赖编译为 ESM 格式；
   - 将编译后的产物缓存到 \`node_modules/.vite\` 目录，下次启动复用缓存。`;export{_ as default};
