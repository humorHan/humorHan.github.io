const n="#### 请说明 TypeScript 中 `unknown`、`any`、`never` 三种类型的区别，以及各自的适用场景。\n\n**答案**：\n\n**示例**：\n\ntypescript\n\n```typescript\n// any：无类型检查const a: any = 'hello';\na.foo(); // 不报错（时才会报错）// unknown：必须收窄类型const b: unknown = 'world';if (typeof b === 'string') {console.log(b.length); // 仅收窄后可操作}// never：永不返回的函数function throwError(): never {throw new Error('错误');}\n```";export{n as default};
