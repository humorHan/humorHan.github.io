const n=`## 5. 浏览器输入 URL 到页面渲染完成的整个网络请求过程

### 核心步骤（含前端核心关注环节）

1. **URL 解析**：校验 URL 合法性，拆分协议（http/https）、域名、端口、路径、参数。
2. **DNS 解析**：如上题，将域名转为 IP 地址（含缓存查询）。
3. **建立连接**：

   - HTTP：TCP 三次握手；
   - HTTPS：TCP 三次握手 + TLS 握手（协商加密算法、证书验证、密钥交换）。
4. **发送 HTTP 请求**：浏览器构造请求行（方法、路径、版本）、请求头（Cookie、User-Agent 等）、请求体，发送到服务器。
5. **服务器处理**：接收请求 → 路由匹配 → 业务逻辑处理 → 构造响应（状态码、响应头、响应体）。
6. **响应返回**：服务器发送响应数据，若为长连接则保持 TCP 连接，短连接则四次挥手。
7. **页面渲染**：

   - 解析 HTML → 构建 DOM 树；
   - 解析 CSS → 构建 CSSOM 树；
   - 合并 DOM+CSSOM → 渲染树（Render Tree）；
   - 布局（Layout）：计算元素位置 / 大小；
   - 绘制（Paint）：像素级绘制；
   - 合成（Composite）：分层渲染，展示到屏幕。
8. **额外环节**：重定向（3xx 状态码）、缓存校验（强缓存 / 协商缓存）、资源加载（JS/CSS/ 图片）、JS 执行（可能阻塞渲染）。`;export{n as default};
