const n=`## 3. 简述 HTTPS 的加密原理，TLS 握手的完整过程是什么？

### 问题核心

考察 HTTPS 核心机制及 TLS 握手的关键步骤，区分对称 / 非对称加密的作用。

### 参考答案

#### （1）HTTPS 加密原理

HTTPS = HTTP + TLS/SSL 协议，核心是**混合加密机制**（结合对称加密和非对称加密的优势）：

1. **非对称加密**：用于握手阶段协商「会话密钥」，解决对称加密密钥传输的安全问题（公钥加密、私钥解密）。
2. **对称加密**：用于后续数据传输，效率高，解决非对称加密速度慢的问题（双方用同一会话密钥加密解密）。
3. **数字证书**：由 CA 机构颁发，验证服务端身份，防止中间人伪造公钥。

#### （2）TLS 1.3 完整握手过程（主流版本）

1. **客户端 Hello**：客户端向服务端发送：支持的 TLS 版本、加密套件列表、随机数（Client Random）、扩展字段（如 ALPN）。
2. **服务端 Hello + 证书 + 密钥交换**：

   - 服务端回复：确认 TLS 版本、加密套件、随机数（Server Random）；
   - 发送数字证书（包含服务端公钥、域名、CA 签名）；
   - 发送密钥交换信息（如 ECDHE 公钥参数）。
3. **客户端验证证书 + 生成会话密钥**：

   - 验证证书合法性（链校验、有效期、域名匹配）；
   - 用服务端公钥加密「预主密钥」（Pre-Master Secret）并发送给服务端；
   - 客户端通过 Client Random + Server Random + Pre-Master Secret 生成「会话密钥」（Master Secret）。
4. **服务端生成会话密钥**：服务端用私钥解密 Pre-Master Secret，生成相同的会话密钥。
5. **握手完成**：双方发送「Finished」消息（用会话密钥加密），确认握手成功；后续所有 HTTP 数据均用会话密钥对称加密传输。`;export{n as default};
