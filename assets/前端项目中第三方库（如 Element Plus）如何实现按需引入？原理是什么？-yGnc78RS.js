const n="### 17. 前端项目中第三方库（如 Element Plus）如何实现按需引入？原理是什么？\n	**答：**\n	#### 一、按需引入实现方式（以 Vue3 + Element Plus 为例）\n	##### 1. 手动按需引入（基础方式）\n	javascript\n	运行\n	```javascript\n// 仅引入需要的组件import { ElButton, ElInput } from 'element-plus'import 'element-plus/es/components/button/style/css'import 'element-plus/es/components/input/style/css'const app = createApp(App)\napp.use(ElButton).use(ElInput)\n````\n\n````\n##### 2. 自动按需引入（推荐）\n- **安装依赖**：`pnpm add -D unplugin-vue-components unplugin-auto-import`\n- **配置 Vite**：\n```javascript\n````\n\n// vite.config.tsimport { defineConfig } from 'vite'import AutoImport from 'unplugin-auto-import/vite'import Components from 'unplugin-vue-components/vite'import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'export default defineConfig({plugins: [AutoImport({resolvers: [ElementPlusResolver()] // 自动导入 Element Plus 方法}),Components({resolvers: [ElementPlusResolver()] // 自动导入 Element Plus 组件})]})\n\n````\n	- **使用**：直接在模板中写 `<el-button>`，无需手动 import。\n	#### 二、按需引入核心原理\n	1. **库的模块化设计**：第三方库需按组件拆分目录（如 `element-plus/es/components/button`），每个组件独立导出；\n	2. **插件解析逻辑**：\n		- `unplugin-vue-components` 会扫描模板中的组件标签（如 `<el-button>`），自动生成 `components.d.ts`，并引入对应组件；\n		- `unplugin-auto-import` 扫描代码中使用的 API（如 `ElMessage`），自动导入对应的方法；\n	3. **Tree Shaking 兜底**：未被引入的组件代码会被 Tree Shaking 移除，最终产物仅包含使用的组件。";export{n as default};
