const _=`## 13. 长连接和短连接的区别是什么？HTTP/1.1 为什么默认开启长连接？

### 长连接 vs 短连接（基于 TCP）

### HTTP/1.1 默认开启长连接（Keep-Alive）的原因

1. **降低性能开销**：HTTP/1.0 每次请求都要建立 TCP 三次握手、响应后四次挥手，开销极大；长连接减少握手 / 挥手次数，提升传输效率；
2. **适配网页特性**：现代网页包含大量静态资源（JS/CSS/ 图片），长连接可复用同一 TCP 连接加载多个资源，减少延迟；
3. **平衡资源占用**：长连接通过 \`Keep-Alive\` 超时时间（如 \`Keep-Alive: timeout=5\`），闲置超时后关闭，避免连接长期占用服务器资源。`;export{_ as default};
