const _=`## 9. 网络请求中出现丢包该如何处理？TCP 是如何保证可靠传输的？

### 丢包处理方式

1. **TCP 层面**：

   - 超时重传：发送方未收到 ACK，超时后重传丢包数据；
   - 快速重传：接收方收到乱序包，连续发送 3 个重复 ACK，发送方立即重传（无需等待超时）；
   - 拥塞控制：慢启动、拥塞避免、快速恢复，避免因网络拥塞加剧丢包。
2. **应用层**：

   - 重试机制（指数退避）：前端对关键请求设置重试，间隔逐渐增大；
   - 分片传输：大文件分片，丢包仅重传分片，不重传整体。

### TCP 保证可靠传输的核心手段

1. **校验和**：计算数据校验和，接收方验证，发现错误则丢弃（触发重传）；
2. **序列号 / 确认应答（ACK）**：每个数据包带序列号，接收方按序接收，返回 ACK 确认（未收到 ACK 则重传）；
3. **重传机制**：超时重传 + 快速重传，解决丢包问题；
4. **流量控制（滑动窗口）**：接收方告知发送方自己的接收窗口大小，发送方控制发送速率，避免接收方处理不过来；
5. **拥塞控制**：避免发送速率超过网络承载能力，减少丢包；
6. **报文段排序**：接收方将乱序数据包排序，保证数据有序；
7. **重复数据丢弃**：通过序列号识别重复包，丢弃冗余数据。`;export{_ as default};
