const _="#### 请实现一个类型工具 `TupleToObject<T>`，将一个字符串 / 数字字面量类型的元组转换为对象类型，元组的元素作为对象的键和值（阿里笔试题）。\n\n**答案**：\n\n```typescript\n// 限制 T 为字符串/数字字面量类型的元组type TupleToObject<T extends readonly (string | number)[]> = {[K in T[number]]: K;};// 测试const tuple = ['name', 'age', 'gender'] as const; // 只读元组（字面量类型）type TupleObj = TupleToObject<typeof tuple>;// TupleObj 类型：{ name: 'name'; age: 'age'; gender: 'gender' }const numTuple = [1, 2, 3] as const;type NumTupleObj = TupleToObject<typeof numTuple>;// NumTupleObj 类型：{ 1: 1; 2: 2; 3: 3 }\n```";export{_ as default};
