const n='### HTML5 audio/video 标签的核心属性 / 事件？如何处理兼容性？\n\n**答案**：\n\n#### 核心属性：\n\n- `src`：媒体文件地址；\n- `controls`：显示原生控制栏（播放 / 暂停 / 音量 / 进度）；\n- `autoplay`：自动播放（需满足浏览器静音策略，如 `muted`）；\n- `loop`：循环播放；\n- `muted`：静音播放；\n- `poster`（video 专属）：视频封面图；\n- `preload`：预加载策略（none/metadata/auto）；\n- `width/height`（video 专属）：视频尺寸。\n\n#### 核心事件：\n\n- `play`：开始播放时触发；\n- `pause`：暂停时触发；\n- `ended`：播放结束时触发；\n- `timeupdate`：播放进度变化时触发；\n- `error`：加载失败时触发；\n- `loadedmetadata`：媒体元数据（时长 / 尺寸）加载完成时触发。\n\n#### 兼容性处理：\n\n1. 提供降级方案：在 audio/video 标签内添加备选内容（如下载链接、Flash 播放器）；\n\n```html\n<video controls>\n  <source src="video.mp4" type="video/mp4" />\n  <source src="video.webm" type="video/webm" />\n  您的浏览器不支持HTML5视频，请<a href="video.mp4">下载视频</a>。\n</video>\n```\n\n1. 检测格式支持：通过 `canPlayType()` 方法判断浏览器是否支持指定格式；\n2. 第三方播放器：使用 Video.js、Plyr 等封装库，统一兼容各浏览器。';export{n as default};
