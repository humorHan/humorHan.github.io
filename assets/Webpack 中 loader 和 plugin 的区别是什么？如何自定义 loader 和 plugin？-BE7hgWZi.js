const n="### 16. Webpack 中 loader 和 plugin 的区别是什么？如何自定义 loader 和 plugin？\n	**答：**\n	#### 一、loader vs plugin 核心区别\n		#### 二、自定义 loader（以去除 console 为例）\n	1. **创建 loader 文件**：`src/loaders/remove-console-loader.js`\n	```javascript\nmodule.exports = function (source) {// source 是源文件内容（字符串）return source.replace(/console\\.(log|warn|error)\\(.*?\\);?/g, '')}\n````\n\n````\n1. **配置 Webpack**：\n```javascript\n````\n\n// webpack.config.js\nmodule.exports = {module: {rules: [{test: /\\.js$/,use: [path.resolve(__dirname, 'src/loaders/remove-console-loader.js')]}]}}\n\n````\n	#### 三、自定义 plugin（以生成构建日志为例）\n	1. **创建 plugin 文件**：`src/plugins/build-log-plugin.js`\n	```javascript\nclass BuildLogPlugin {apply(compiler) {// 监听 compile 钩子（编译开始时）\n    compiler.hooks.compile.tap('BuildLogPlugin', () => {console.log('编译开始时间：', new Date().toLocaleString())})// 监听 done 钩子（编译完成时）\n    compiler.hooks.done.tap('BuildLogPlugin', (stats) => {const { compilation } = stats\n      console.log('编译完成时间：', new Date().toLocaleString())console.log('产物体积：', compilation.outputPath)})}}\n\nmodule.exports = BuildLogPlugin\n````\n\n````\n1. **配置 Webpack**：\n```javascript\n````\n\nconst BuildLogPlugin = require('./src/plugins/build-log-plugin.js')\nmodule.exports = {plugins: [new BuildLogPlugin()]}\n\n````";export{n as default};
