const _=`### 11. HTTP2 的多路复用原理是什么？它如何解决 HTTP1.1 的队头阻塞问题？

#### 多路复用原理

HTTP2 基于 **二进制帧** 和 **流（Stream）** 实现多路复用：

1. **二进制帧**：将请求 / 响应拆分为多个二进制帧（最小传输单元），每个帧包含流 ID、类型、数据；
2. **流**：每个请求 / 响应对应一个独立的流（唯一流 ID），所有流在**同一个 TCP 连接**上并行传输；
3. **乱序传输 / 重组**：服务器可乱序返回不同流的帧，客户端根据流 ID 将帧重组为完整的请求 / 响应。

#### 解决队头阻塞问题

- **HTTP1.1 队头阻塞**：同一 TCP 连接下，请求按 “FIFO” 顺序执行，前一个请求阻塞（如慢响应、丢包），后续所有请求都需等待；
- **HTTP2 解决方式**：多路复用允许同一 TCP 连接下多个流并行传输，某一流的帧阻塞（如丢包），仅影响该流，其他流可正常传输，彻底解决应用层队头阻塞（TCP 层队头阻塞仍存在）。`;export{_ as default};
