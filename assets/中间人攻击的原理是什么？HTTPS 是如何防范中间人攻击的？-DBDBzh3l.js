const n=`## 8. 中间人攻击的原理是什么？HTTPS 是如何防范中间人攻击的？

### 问题核心

考察中间人攻击的本质，以及 HTTPS 抵御该攻击的核心机制。

### 参考答案

#### （1）中间人攻击（MITM）原理

攻击者拦截客户端与服务端之间的通信链路，伪装成「客户端的服务端」和「服务端的客户端」，实现：

1. 窃取：获取双方传输的明文数据（如账号密码）；
2. 篡改：修改请求 / 响应内容（如转账金额）；
3. 伪造：冒充服务端向客户端返回虚假数据。核心是**打破客户端与服务端的直接通信，成为数据传输的 “中间代理”**。

#### （2）HTTPS 防范中间人攻击的核心机制

1. **数字证书验证服务端身份**：

   - 服务端的公钥由 CA 机构签名生成数字证书，客户端会验证证书的合法性（是否被篡改、是否在有效期、域名是否匹配、CA 是否可信）；
   - 中间人无法伪造合法的数字证书（无 CA 私钥），客户端验证失败会提示风险，终止连接。
2. **非对称加密协商会话密钥**：

   - 握手阶段的预主密钥仅能由服务端私钥解密，中间人无法获取，因此无法生成相同的会话密钥；
   - 后续数据传输用会话密钥对称加密，中间人即使拦截数据也无法解密。
3. **数据完整性校验**：

   - 传输的数据会附加 MAC（消息认证码），接收方验证 MAC 确认数据未被篡改，中间人修改数据后 MAC 会失效。
4. **证书链校验**：客户端会验证证书的签发链（从服务端证书到根 CA 证书），防止中间人伪造自签名证书。`;export{n as default};
