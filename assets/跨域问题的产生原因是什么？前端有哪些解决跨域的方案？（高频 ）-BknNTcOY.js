const n=`### 跨域问题的产生原因是什么？前端有哪些解决跨域的方案？（高频 ）

答：方案分「前端方案」「后端方案」「代理方案」，条理清晰。

#### 一、跨域的产生原因

浏览器的 **同源策略** 限制导致跨域，同源策略要求：页面的「协议、域名、端口」三者必须完全一致，只要有一个不一致，就会被判定为跨域，此时浏览器会拦截异步请求（Ajax/fetch/axios），控制台报 \`CORS policy\` 错误。

> 补充：同源策略的目的是为了保护用户信息安全，防止恶意网站窃取数据。

#### 二、前端常用的跨域解决方案（按优先级 / 实用性排序，答出前 5 种即满分）

1. ✅ 方案 1（后端主导，最优，项目首选）：**后端设置 CORS 跨域**，在响应头添加配置：\`Access-Control-Allow-Origin: *\`，允许所有域名访问，前端无需任何修改，最简单高效。

2. ✅ 方案 2（前端主导，开发环境首选）：**webpack 配置代理（proxy）**，本地开发时用，原理是：前端请求代理服务器，代理服务器再请求后端接口，避开浏览器同源限制。

3. javascript

4. 运行

\`\`\`

// vue.config.js 配置示例 devServer: { proxy: { '/api': { target: 'http://后端接口地址', changeOrigin: true, pathRewrite: {'^/api': ''} } } }

\`\`\`

1. ✅ 方案 3：**JSONP 跨域**，原理是利用 \`<script>\` 标签不受同源策略限制，缺点：只支持 GET 请求，不支持 POST/PUT/DELETE，适合老项目兼容。

2. ✅ 方案 4：**nginx 反向代理**，生产环境首选，部署时用 nginx 做中间层，前端请求 nginx，nginx 转发请求到后端，实现跨域。

3. ✅ 方案 5：**postMessage**，适用于「不同域名的页面之间通信」，比如 iframe 嵌套页面、父子页面通信。

4. 其他方案：WebSocket（双向通信，无跨域限制）、Node.js 中间层代理，社招补充即可。`;export{n as default};
