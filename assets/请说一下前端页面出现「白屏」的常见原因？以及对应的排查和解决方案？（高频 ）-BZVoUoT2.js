const _=`### 请说一下前端页面出现「白屏」的常见原因？以及对应的排查和解决方案？（高频 ）

答：这是大厂最爱考的**综合场景题**，分「开发环境白屏」和「生产环境白屏」两类，原因 + 解决方案对应回答，逻辑清晰：

#### 一、开发环境页面白屏（本地启动项目）

1. 原因：代码语法报错、引入的第三方包报错、路由匹配失败、Vue/React 生命周期执行报错、端口被占用；
2. 排查方案：打开浏览器控制台（F12）→ 看 Console 面板的报错信息，定位报错代码行；看 Network 面板，检查资源是否加载成功。

#### 二、生产环境页面白屏（项目打包上线后）

1. 核心原因：打包后的 JS/CSS 文件加载失败、打包代码压缩时语法被破坏、接口请求失败 / 跨域、路由懒加载失败、浏览器兼容性问题（ES6 + 语法未转 ES5）、Vue/React 的根节点挂载失败；
2. 排查 + 解决方案：① 看控制台报错，优先解决语法错误、资源 404 错误；② 打包时配置 \`babel-polyfill/core-js\`，兼容低版本浏览器的 ES6 + 语法；③ 配置路由懒加载兜底，避免路由匹配失败导致白屏；④ 接口请求加异常捕获，接口失败时给出页面兜底（加载失败提示）；⑤ 打包时关闭 \`productionSourceMap\`，避免打包体积过大加载慢导致的白屏；⑥ 配置 CDN 加速静态资源，避免资源加载超时。

> 加分项：补充「首屏白屏优化」→ 加骨架屏 / 加载动画、路由懒加载、资源按需引入、预加载核心资源，提升用户体验。`;export{_ as default};
