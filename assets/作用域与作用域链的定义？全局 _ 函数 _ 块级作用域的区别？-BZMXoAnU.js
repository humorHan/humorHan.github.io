const n=`### 5. 作用域与作用域链的定义？全局 / 函数 / 块级作用域的区别？

**答案**：

#### 1. 作用域（Scope）：

指变量 / 函数的可访问范围，决定了代码中变量的可见性和生命周期，核心作用是隔离变量，避免命名冲突。

#### 2. 作用域链（Scope Chain）：

当访问变量时，JS 引擎会先在当前作用域查找，若找不到则向上查找外层作用域，直到全局作用域，这个链式查找的结构就是作用域链。作用域链的顶端是全局作用域，底端是当前执行上下文的作用域。

#### 3. 全局 / 函数 / 块级作用域的区别：

#### 示例：

js

\`\`\`javascript
// 全局作用域var a = 10;function fn() {// 函数作用域var b = 20;console.log(a); // 可访问全局变量aif (true) {// 块级作用域let c = 30;console.log(b); // 可访问函数作用域变量b}console.log(c); // 报错：c is not defined（块级作用域外部不可访问）}fn();console.log(b); // 报错：b is not defined（函数作用域外部不可访问）
\`\`\``;export{n as default};
