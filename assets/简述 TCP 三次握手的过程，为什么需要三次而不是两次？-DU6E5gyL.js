const _=`## 2. 简述 TCP 三次握手的过程，为什么需要三次而不是两次？

### 三次握手过程

1. **第一次握手（SYN）**：客户端 → 服务端，发送 SYN 包（同步序列号），声明客户端的初始序列号 ISN (c)，进入 SYN_SENT 状态。
2. **第二次握手（SYN+ACK）**：服务端 → 客户端，发送 SYN 包（声明服务端初始序列号 ISN (s)）+ ACK 包（确认客户端的 SYN，ACK=ISN (c)+1），进入 SYN_RCVD 状态。
3. **第三次握手（ACK）**：客户端 → 服务端，发送 ACK 包（确认服务端的 SYN，ACK=ISN (s)+1），双方进入 ESTABLISHED 状态，连接建立。

### 为什么需要三次（核心：确认双方收发能力）

- 两次握手仅能确认「客户端能发、服务端能收 + 能发」，但**服务端无法确认客户端能接收自己的消息**（客户端收不到服务端的 SYN+ACK，服务端永远不知道客户端是否准备好）。
- 三次握手可完整验证：客户端（发 + 收）、服务端（收 + 发）的双向通信能力，避免「服务端建立无效连接」（如客户端 SYN 包超时，服务端却等待客户端数据，浪费资源）。`;export{_ as default};
