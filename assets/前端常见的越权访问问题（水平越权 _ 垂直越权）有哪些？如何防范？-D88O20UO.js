const _=`## 10. 前端常见的越权访问问题（水平越权 / 垂直越权）有哪些？如何防范？

### 问题核心

考察越权攻击的类型、场景及核心防范手段（重点：前端鉴权不可靠，核心靠后端）。

### 参考答案

#### （1）越权访问的定义及常见场景

#### （2）防范手段（核心靠后端，前端仅辅助）

##### 后端核心防范（重中之重）

1. **严格的权限校验**：

   - 垂直越权：每个接口校验用户角色（如管理员 / 普通用户），仅允许对应角色访问；
   - 水平越权：每个请求验证资源归属（如订单的 user_id 是否等于当前登录用户 ID），**禁止信任前端传入的用户 ID**（从会话中获取真实用户 ID）。
2. **最小权限原则**：为用户分配最小必要权限（如普通用户仅能查自己的订单，不能删）。
3. **参数校验**：对请求参数（如用户 ID、资源 ID）做合法性校验，拒绝非法参数（如负数、不存在的 ID）。
4. **隐藏敏感接口**：高权限接口使用非猜解型路径（如 \`/api/v1/admin/89a7s9d8\` 而非 \`/api/v1/admin/1\`）。
5. **日志审计**：记录越权访问行为，及时发现异常攻击。

##### 前端辅助防范

1. **隐藏敏感功能入口**：低权限用户隐藏管理员按钮、菜单，避免诱导攻击（仅辅助，不能替代后端校验）。
2. **请求前校验**：前端对用户权限做基础判断，避免无意义的越权请求（如普通用户点击管理员按钮时提示无权限）。
3. **避免暴露敏感参数**：不在 URL 中暴露用户 ID、资源 ID 等敏感参数（改用 POST 请求体传递）。
4. **禁用前端调试工具的接口重放**：辅助降低攻击概率（核心仍靠后端）。`;export{_ as default};
