const e="#### 请实现一个类型工具 `CapitalizeKeys<T>`，将对象 `T` 的所有属性名转换为首字母大写的形式（如 `{ name: string }` 转换为 `{ Name: string }`）。\n\n**答案**：\n\n```typescript\n// 辅助类型：首字母大写type CapitalizeStr<S extends string> = S extends `${infer First}${infer Rest}`? `${Uppercase<First>}${Rest}`: S;// 核心类型：遍历属性并转换名称type CapitalizeKeys<T> = {[K in keyof T as CapitalizeStr<K & string>]: T[K];};// 测试type User = {\n  name: string;\n  age: number;\n  isAdmin: boolean;};type CapitalizedUser = CapitalizeKeys<User>;// CapitalizedUser 类型：{ Name: string; Age: number; IsAdmin: boolean }\n```";export{e as default};
