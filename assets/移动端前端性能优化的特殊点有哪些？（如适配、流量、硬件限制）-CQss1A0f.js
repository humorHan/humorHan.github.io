const n=`### 移动端前端性能优化的特殊点有哪些？（如适配、流量、硬件限制）

**答案：**移动端性能优化需适配 “弱网络、低性能硬件、有限流量、多样屏幕” 等特点，核心特殊点如下：

#### 一、网络层面（适配弱网络）

1. **减少资源体积，降低流量消耗**
	- 图片优先用 WebP/AVIF（比 JPG 小 50%），禁用超大图片；
	- 开启 Gzip/Brotli 压缩（服务端配置），压缩 JS/CSS/ 接口数据；
	- 接口数据精简：只返回必要字段，避免冗余数据（如列表页只返回标题 / 图片，详情页再请求完整数据）。

2. **优化请求策略**
	- 接口请求防抖 / 节流（如搜索框输入，避免频繁请求）；
	- 离线缓存：用 Service Worker/IndexedDB 缓存接口数据，弱网 / 无网时展示缓存内容；
	- 延迟加载非关键请求：首屏加载完成后，再请求非核心数据（如推荐列表）；
	- 避免并行请求过多：移动端网络并发能力弱，控制同时请求数（≤4 个）。

#### 二、硬件层面（适配低性能设备）

1. **降低计算 / 渲染压力**
	- 避免复杂 CSS 效果：如 \`box-shadow\`、\`filter\`、渐变（消耗 GPU），简化动画；
	- 减少重排 / 重绘：移动端 DOM 操作更敏感，批量修改 DOM，用 \`transform/opacity\` 做动画；
	- 避免长任务：移动端 CPU 性能弱，将耗时计算移到 Web Worker，拆分长任务为小任务。

2. **内存优化**
	- 及时释放资源：卸载组件时清理事件监听、定时器、Observer；
	- 避免大数组 / 大对象常驻内存：如列表数据分页加载，只保留当前页数据；
	- 图片懒加载：默认加载低分辨率图，进入视口再加载高清图。

#### 三、适配层面（多样屏幕 / 交互）

1. **屏幕适配优化**
	- 采用 REM/VW 适配（而非固定像素），避免元素溢出 / 压缩；
	- 图片用响应式方案：根据屏幕宽度加载不同尺寸图片（如 375px 屏加载 750px 图，750px 屏加载 1500px 图）；
	- 禁用横向滚动：移动端横向滚动体验差，布局适配确保纵向滚动。

2. **交互适配优化**
	- 点击区域放大：移动端手指点击精度低，按钮 / 可点击元素宽高≥44px；
	- 避免 300ms 点击延迟：使用 \`touchstart\` 替代 \`click\`（需处理穿透），或引入 \`fastclick\` 库；
	- 优化触摸反馈：如点击按钮添加轻微缩放动画，提升交互感知，避免动画过度（卡顿）。

#### 四、其他特殊优化点

1. **电量优化**：减少频繁的网络请求、GPU 渲染、定时器（如 \`setInterval\` 改为 \`requestAnimationFrame\`），降低耗电；

2. **启动速度优化**：
	- 首屏骨架屏：减少白屏感知，适配移动端启动慢的问题；
	- 预加载核心资源：如首屏 CSS/JS 内联，减少请求；

3. **兼容低版本浏览器**：移动端 WebView 版本参差不齐，避免使用新 API（如 \`IntersectionObserver\` 需降级），核心功能做兼容处理；

4. **避免弹窗干扰**：移动端屏幕小，非必要不弹出弹窗，弹窗需支持一键关闭。`;export{n as default};
