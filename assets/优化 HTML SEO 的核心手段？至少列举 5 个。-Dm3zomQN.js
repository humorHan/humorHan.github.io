const n="### 优化 HTML SEO 的核心手段？至少列举 5 个。\n\n**答案**：\n\n1. 语义化标签：使用 `<header>/<article>/<main>` 等标签，让搜索引擎解析内容结构；\n2. 合理的标题层级：使用 `<h1>-<h6>`（一个页面仅一个 `<h1>`，层级连续不跳级）；\n3. meta 优化：配置 `description`（简洁描述页面内容）、`keywords`（核心关键词）；\n4. 图片优化：给 `<img>` 添加 `alt` 属性（描述图片内容，搜索引擎可识别），`title` 属性补充说明；\n5. 链接优化：`<a>` 标签使用有意义的锚文本（如 “前端面试题” 而非 “点击这里”），避免空链接 / 死链接；\n6. 结构化数据：使用 Microdata/[Schema.org](https://schema.org/) 添加微数据，提升富摘要展示；\n7. 减少 iframe：搜索引擎难以解析 iframe 内容，尽量避免用 iframe 承载核心内容；\n8. 静态 URL：使用简洁、包含关键词的 URL（如 `/frontend/interview` 而非 `/?id=123`）；\n9. 提升页面加载速度：减少 HTTP 请求、压缩 HTML，搜索引擎优先收录加载快的页面；\n10. 移动端适配：采用响应式布局，移动端体验差会影响排名。";export{n as default};
