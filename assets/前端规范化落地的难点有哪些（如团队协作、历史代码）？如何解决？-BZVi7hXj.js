const t=`### 20. 前端规范化落地的难点有哪些（如团队协作、历史代码）？如何解决？
**答：**
#### 一、规范化落地核心难点
	#### 二、解决策略
1. **降低执行成本（自动化）**：
	- 集成 Husky + lint-staged，提交代码自动格式化 / 校验；
	- 配置 IDE 自动格式化（如 VS Code 安装 ESLint/Prettier 插件，开启保存自动格式化）；
	- 制定统一的配置文件（\`.eslintrc.js\`、\`.prettierrc\`），提交到 Git 共享。
2. **分阶段整改历史代码**：
	- 新代码严格遵守规范，老代码在迭代时逐步整改；
	- 对核心模块优先整改，非核心模块暂缓；
	- 使用 \`eslint --fix\` 批量自动修复可格式化的问题。
3. **统一认知和共识**：
	- 组织规范培训，讲解规则背后的原因（如为什么禁用 \`var\`）；
	- 制定规范文档，明确例外情况（如特殊场景可临时禁用某条 ESLint 规则）；
	- 成立规范小组，负责答疑和规则迭代。
4. **柔性执行，避免一刀切**：
	- 初期放宽部分规则，逐步收紧；
	- 对违规代码不强制回滚，而是记录并提醒；
	- 将规范执行情况纳入团队考核（非惩罚性，侧重引导）。
5. **工具兜底，减少人为干预**：
	- 使用 CI 流程强制校验，不通过则禁止合并代码；
	- 配置 ESLint 规则为 “警告” 而非 “错误”，逐步过渡；
	- 定期输出规范执行报告，表扬合规率高的开发者。`;export{t as default};
