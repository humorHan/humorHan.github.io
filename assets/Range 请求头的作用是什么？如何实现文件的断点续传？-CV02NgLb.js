const n=`### 13. Range 请求头的作用是什么？如何实现文件的断点续传？

#### Range 头作用

客户端请求服务器返回资源的**指定字节范围**，用于：

- 断点续传（下载中断后继续下载）；
- 分片下载（多线程下载大文件）；
- 按需加载（如视频播放时加载指定片段）。

#### 断点续传实现流程

1. **记录已下载范围**：客户端下载文件时，记录已下载的字节数（如 0-10240）；
2. **发送 Range 请求**：再次请求时，添加请求头 \`Range: bytes=已下载字节-\`（如 \`Range: bytes=10240-\`）；
3. **服务器响应**：

   - 验证 Range 合法，返回 206 Partial Content 状态码；
   - 响应头携带 \`Content-Range: bytes 10240-20480/102400\`（当前范围 / 总大小）；
4. **客户端拼接文件**：将新下载的字节数据追加到已下载的文件中，直到下载完成。`;export{n as default};
