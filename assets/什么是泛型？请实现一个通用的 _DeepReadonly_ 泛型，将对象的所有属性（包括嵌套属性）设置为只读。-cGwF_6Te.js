const _="#### 什么是泛型？请实现一个通用的 `DeepReadonly` 泛型，将对象的所有属性（包括嵌套属性）设置为只读。\n\n**答案**：泛型是 TS 的 “类型参数”，允许定义可复用的类型 / 函数，支持传入不同类型参数而不丢失类型信息，核心是 “参数化类型”。\n\n`DeepReadonly` 实现：\n\n```typescript\ntype DeepReadonly<T> = {readonly [K in keyof T]: \n    T[K] extends object // 判断是否为对象/数组? T[K] extends Function // 排除函数（函数无需只读）? T[K]: DeepReadonly<T[K]> // 递归处理嵌套属性: T[K];};// 测试type TestObj = { a: number; b: { c: string } };type ReadonlyObj = DeepReadonly<TestObj>;// ReadonlyObj 类型：{ readonly a: number; readonly b: { readonly c: string } }\n```";export{_ as default};
