const e='### 25. TypeScript 在前端工程化中的集成要点是什么？如何配置 tsconfig.json 适配 Vue3 项目？\n**答：**\n#### 一、TypeScript 集成核心要点\n1. **环境搭建**：\n	- 安装依赖：`pnpm add -D typescript @vue/tsconfig @types/node`；\n	- 初始化 `tsconfig.json`：`npx tsc --init`。\n2. **框架适配**：\n	- Vue3：安装 `@vitejs/plugin-vue-jsx`、`vue-tsc`（类型检查）；\n	- React：安装 `@types/react`、`@types/react-dom`；\n	- 配置类型声明文件（`*.d.ts`），解决第三方库无类型问题。\n3. **构建工具集成**：\n	- Vite：天然支持 TS，无需额外配置；\n	- Webpack：使用 `ts-loader` 或 `babel-loader`（需 `@babel/preset-typescript`）。\n4. **规范校验**：\n	- 集成 ESLint：安装 `@typescript-eslint/eslint-plugin`、`@typescript-eslint/parser`；\n	- 开启严格模式（`strict: true`），提前发现类型错误。\n#### 二、Vue3 项目 tsconfig.json 最佳配置\njson\n```json\n````\n\n{"extends": "@vue/tsconfig/tsconfig.web.json", // 继承 Vue 官方配置"compilerOptions": {"target": "ES2020", // 目标 ES 版本"module": "ESNext", // 模块规范"moduleResolution": "NodeNext", // 模块解析方式"strict": true, // 开启严格类型检查（核心）"jsx": "preserve", // 保留 JSX 交由 Vue 处理"sourceMap": true, // 生成 sourcemap"resolveJsonModule": true, // 支持导入 JSON 文件"esModuleInterop": true, // 兼容 CommonJS 模块"lib": ["ES2020", "DOM"], // 引入的库类型"baseUrl": ".", // 基础路径"paths": { // 路径别名"@/_": ["src/_"]},"types": ["vite/client", "vue-router", "pinia"] // 全局类型},"include": [ // 包含的文件"src/**/*.ts","src/**/*.d.ts","src/**/*.tsx","src/**/*.vue"],"exclude": [ // 排除的文件"node_modules","dist","**/*.test.ts"]}\n\n````\n	#### 三、关键配置说明\n	1. **strict: true**：开启 `noImplicitAny`、`strictNullChecks` 等严格规则，是 TS 类型安全的核心；\n	2. **paths 别名**：需与 Vite/Webpack 的别名配置一致，避免路径解析错误；\n	3. **vite/client**：引入 Vite 全局类型（如 `import.meta.env`）；\n	4. **vue-tsc**：执行 `vue-tsc --noEmit` 做类型检查（Vite 构建时不会执行 TS 类型检查）。';export{e as default};
