const _=`### npm、yarn、pnpm 的核心差异（安装机制、性能、锁文件）是什么？大型项目为何优先选 pnpm？

**答：**

#### 核心差异

#### 大型项目优先选 pnpm 的原因

1. **极致性能**：全局 store 缓存依赖，多项目共享，安装速度比 npm/yarn 快 2-3 倍；
2. **零冗余存储**：硬链接 + 符号链接机制，磁盘占用仅为 npm/yarn 的 1/10；
3. **依赖隔离**：杜绝幽灵依赖（未声明的依赖无法被导入），避免依赖冲突；
4. **原生 Monorepo 支持**：内置 workspace，无需额外配置即可管理多包项目；
5. **严格的锁文件**：\`pnpm-lock.yaml\` 格式更清晰，能精准锁定依赖版本和路径；
6. **兼容性好**：完全兼容 npm 包规范，支持 \`.npmrc\` 配置，迁移成本低。`;export{_ as default};
