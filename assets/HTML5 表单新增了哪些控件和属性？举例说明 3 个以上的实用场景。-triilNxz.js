const n='### HTML5 表单新增了哪些控件和属性？举例说明 3 个以上的实用场景。\n\n**答案**：\n\n#### 新增控件：\n\n- `type="email"`：邮箱输入框（自动校验格式）；\n- `type="tel"`：电话输入框（移动端唤起数字键盘）；\n- `type="url"`：网址输入框（校验 URL 格式）；\n- `type="number"`：数字输入框（限制仅输入数字，带增减按钮）；\n- `type="date"/"month"/"time"`：日期 / 月份 / 时间选择器（原生控件，无需第三方插件）；\n- `type="range"`：滑块控件（如音量调节、价格区间）；\n- `<datalist>`：带下拉建议的输入框（绑定 input 的 list 属性）。\n\n#### 新增属性：\n\n- `placeholder`：输入框提示文字；\n- `required`：标记字段为必填；\n- `pattern`：自定义正则校验（如 `pattern="\\d{6}"` 校验 6 位数字）；\n- `autofocus`：页面加载后自动聚焦；\n- `multiple`：允许文件 / 多选框多选（如 `<input type="file" multiple>`）；\n- `maxlength/minlength`：输入内容的最大 / 最小长度。\n\n#### 实用场景：\n\n1. 注册页用 `type="email"`+`required`+`pattern` 校验邮箱格式，减少前端自定义校验成本；\n2. 手机号输入框用 `type="tel"`，移动端唤起数字键盘提升体验；\n3. 生日选择用 `type="date"`，替代第三方日期选择插件，简化代码；\n4. 搜索框绑定 `<datalist>`，提供搜索建议（如历史搜索词）。';export{n as default};
