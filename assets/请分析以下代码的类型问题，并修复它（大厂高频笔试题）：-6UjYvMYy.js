const r="#### 请分析以下代码的类型问题，并修复它（大厂高频笔试题）：\n\n```typescript\nfunction sum(a: number, b: number) {return a + b;}const arr = [1, 2, 3];const result = sum(...arr); // 此处有类型报错，请修复并说明原因\n```\n\n**答案**：**问题原因**：`arr` 的类型被 TS 推断为 `number[]`（任意长度的数字数组），而 `sum` 函数要求传入**恰好 2 个数字参数**，展开任意长度的数组会导致参数数量不匹配，触发类型报错。\n\n**修复方案**（两种）：\n\n```typescript\n// 方案1：明确 arr 为长度2的元组类型（推荐）const arr: [number, number] = [1, 2]; \nconst result = sum(...arr); // 无报错// 方案2：类型断言（适用于确定数组长度的场景）const arr = [1, 2] as [number, number];const result = sum(...arr); // 无报错// 方案3：时校验（兼容动态数组）if (arr.length === 2) {const [a, b] = arr;const result = sum(a, b); // 无报错}\n```";export{r as default};
