const n="#### TypeScript 中 `declare` 关键字的作用是什么？请举例说明在声明第三方无类型库（如老旧 JS 插件）时的使用方式（美团工程化考点）。\n\n**答案**：`declare` 用于**声明已有但未被 TS 识别的类型**（仅编译时生效，无时代码），核心作用是为 JS 代码、第三方库补充类型信息，避免 `找不到名称`/`隐式 any` 报错。\n\n**场景：声明无类型的第三方 JS 库（如 ****lodash-es**** 老版本 / 自定义插件）**：\n\n1. 创建 `src/types/custom.d.ts` 文件（TS 会自动识别 `.d.ts` 声明文件）；\n2. 声明库的类型：\n\n```typescript\n// 1. 声明模块（全局库）declare module 'old-plugin' {// 声明库导出的函数export function formatDate(date: Date | string): string;// 声明库导出的对象export const version: string;}// 2. 声明全局变量（如 window 上的全局方法）declare global {interface Window {\n    $myPlugin: {init: (options: { timeout: number }) => void;};}}\n```\n\n1. 在代码中使用：\n\n```typescript\nimport { formatDate } from 'old-plugin';console.log(formatDate(new Date())); // 有类型提示，无报错\n\nwindow.$myPlugin.init({ timeout: 1000 }); // 全局变量有类型提示\n```";export{n as default};
