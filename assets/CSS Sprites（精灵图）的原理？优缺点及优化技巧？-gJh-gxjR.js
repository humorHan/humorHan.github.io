const n=`### CSS Sprites（精灵图）的原理？优缺点及优化技巧？

**答案**：

#### 原理：

将多个小图标 / 背景图合并为一张大图片，通过 \`background-position\` 定位显示指定区域，减少 HTTP 请求数量（一张图替代多张图）。

#### 优点：

- 减少 HTTP 请求，提升页面加载速度；
- 减少图片总大小（合并后压缩比更高）；
- 便于图标管理（统一修改 / 替换）。

#### 缺点：

- 维护成本高（新增 / 修改图标需重新合并图片，调整定位）；
- 定位精准度要求高（易出现偏移）；
- 不适合大图片 / 动态图片（如 banner）。

#### 优化技巧：

1. 按图标用途分组（如导航图标 / 按钮图标分不同精灵图）；
2. 预留空白（避免图标重叠）；
3. 使用工具自动生成（如 SpriteSmith、在线精灵图生成工具）；
4. 配合 CSS 变量管理定位值（便于维护）；
5. 响应式精灵图：使用 \`background-size\` 适配不同屏幕；
6. 逐步替换：现代项目优先用 iconfont（字体图标）替代精灵图。`;export{n as default};
