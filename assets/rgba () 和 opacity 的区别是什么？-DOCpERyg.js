const a="### rgba () 和 opacity 的区别是什么？\n\n答：两者都是 CSS 中设置**透明度**的核心属性，是 2025 年大厂高频易混淆考点，**面试必须答全 4 个核心区别，缺一不可**，标准答案如下：\n\n1. **作用范围不同**\n\n   - `rgba()`：是**属性级透明度**，只作用于「当前设置的单一属性」，比如 `background: rgba(0,0,0,0.5)` 仅改变背景色透明度，`color:rgba(255,255,255,0.3)` 仅改变文字透明度；\n   - `opacity`：是**元素级透明度**，作用于「整个元素及内部所有子元素」，包括元素的背景、文字、边框、图片、子节点等全部内容。\n2. **继承性不同**\n\n   - `rgba()`：**无继承性**，只会影响自身设置的属性，子元素的样式完全不受影响；\n   - `opacity`：**完全继承**，父元素设置 opacity 后，所有子元素会继承相同的透明度，且子元素**无法通过自身样式取消该透明度**。\n3. **取值与使用方式不同**\n\n   - `rgba()`：格式为 `rgba(r,g,b,alpha)`，前三位是 RGB 色值，最后一位 alpha 是透明度（0~1），**必须依附于颜色属性使用**（background/color/border-color 等）；\n   - `opacity`：单独作为 CSS 属性使用，取值 0~1，直接写在元素上即可，比如 `opacity:0.6`。\n4. **实际开发场景不同**\n\n   - 需求是「只让背景透明，文字不透明」→ 用 `rgba()`，这是开发中最常用的场景（如弹窗遮罩、按钮 hover 背景）；\n   - 需求是「让整个元素及内部所有内容一起透明」→ 用 `opacity`，比如元素的淡出动画、悬浮半透效果。\n\n> 经典举例：一个按钮设置 `background: rgba(0,0,0,0.4)`，按钮文字是完全不透明的；如果设置 `opacity:0.4`，按钮背景 + 文字会一起变透明。";export{a as default};
