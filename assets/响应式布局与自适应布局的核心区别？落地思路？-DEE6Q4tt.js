const _=`### 响应式布局与自适应布局的核心区别？落地思路？

**答案**：

#### 落地思路：

##### 响应式布局：

1. 设置 \`viewport\`：\`width=device-width, initial-scale=1.0\`；
2. 使用弹性单位（百分比 /rem/vw）替代固定像素；
3. 媒体查询微调样式（如小屏减小字体 / 间距）；
4. 配合 Flex/Grid 布局，实现元素自动适配；
5. 图片响应式（\`max-width: 100%\`，\`srcset\` 加载不同尺寸图片）。

##### 自适应布局：

1. 定义断点（如 320px/480px/768px/1200px）；
2. 每个断点写一套固定宽度的布局（如 \`<768px\` 时宽度 100%，\`≥768px\` 时宽度 750px，\`≥1200px\` 时宽度 1140px）；
3. 媒体查询切换不同断点的样式；
4. 适合对不同设备有差异化设计的场景（如移动端 / PC 端完全不同的布局）。`;export{_ as default};
