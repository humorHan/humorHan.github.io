const T=`## 10. HTTP/1.1、HTTP/2、HTTP/3 各自的核心特性和区别是什么？

### Q1：HTTP/1.1 的核心特性有哪些？

**答：**HTTP/1.1 作为经典且长期主流的版本，核心特性如下：

1. **持久连接（Keep-Alive）**：默认开启，单个 TCP 连接可处理多个 HTTP 请求 / 响应，避免频繁创建 / 销毁 TCP 连接的性能开销（HTTP/1.0 需手动配置该字段）；
2. **请求管道化（Pipelining）**：允许在单个 TCP 连接上并行发送多个请求，无需等待前一个请求的响应返回（但响应需严格按请求顺序返回）；
3. **Host 头字段**：支持单服务器托管多个域名（虚拟主机），解决 HTTP/1.0 中一台服务器仅对应一个域名的问题；
4. **分块传输编码**：支持流式传输动态内容，无需提前知道响应体长度；
5. **完善的缓存机制**：新增 \`Cache-Control\`、\`ETag\`、\`Last-Modified\` 等缓存头，精细化控制缓存策略；
6. **方法扩展**：新增 PUT、DELETE、OPTIONS 等请求方法，适配 REST 语义。

### Q2：HTTP/2 的核心特性有哪些？

**答：**HTTP/2 基于 SPDY 协议演进，核心目标是解决 HTTP/1.1 的性能瓶颈，特性如下：

1. **二进制分帧**：将请求 / 响应拆分为二进制帧（Frame），替代 HTTP/1.1 的文本格式，解析效率更高、错误率更低；
2. **多路复用**：单个 TCP 连接内可同时传输多个请求 / 响应（帧级并行），彻底解决 HTTP/1.1 的 “队头阻塞” 问题；
3. **服务器推送（Server Push）**：服务器可主动向客户端推送静态资源（如 JS/CSS），无需等待客户端请求；
4. **头部压缩（HPACK）**：对重复的 HTTP 头进行压缩（如 User-Agent、Cookie 等），减少传输体积；
5. **流优先级**：可对不同请求流设置优先级，优先传输核心资源（如 HTML 优先于图片）；
6. **兼容性**：向下兼容 HTTP/1.1 的方法、状态码、头字段，仅传输层优化。

### Q3：HTTP/3 的核心特性有哪些？

**答：**HTTP/3 基于 QUIC 协议（UDP 之上的可靠传输协议），核心解决 TCP 层面的问题，特性如下：

1. **基于 UDP 传输**：替代传统 TCP 协议，从根本上解决 TCP 的队头阻塞问题（UDP 无连接，单流故障不影响其他流）；
2. **0-RTT/1-RTT 握手**：TCP 需 3 次握手（3-RTT），QUIC 支持 0-RTT 建立连接，首次连接 1-RTT，大幅降低连接建立延迟；
3. **内置 TLS 1.3**：将 TLS 加密集成到传输层，无需额外握手，进一步减少延迟；
4. **连接迁移**：基于 Connection ID 标识连接，而非 IP + 端口，切换网络（如 WiFi 切 5G）时无需重新建立连接；
5. **多路复用**：基于 UDP 实现更彻底的多路复用，无 TCP 层面的队头阻塞；
6. **可靠性保障**：QUIC 自身实现了 TCP 的可靠传输特性（重传、拥塞控制等），兼顾 UDP 的灵活和 TCP 的可靠。

### Q4：HTTP/1.1、HTTP/2、HTTP/3 的核心区别是什么？

**答：**核心区别可从传输层、性能瓶颈、关键能力三个维度总结：

### Q5：实际应用中，三个版本的适用场景有哪些？

**答：**

1. **HTTP/1.1**：仍广泛用于老旧系统、低性能服务器，或对兼容性要求极高的场景（如适配 IE8 等老旧浏览器）；
2. **HTTP/2**：目前主流应用场景（如电商、官网、小程序），大部分现代浏览器和 CDN 均支持，在 TCP 基础上大幅提升性能，部署成本低；
3. **HTTP/3**：适合对延迟敏感的场景（如直播、实时通信、移动应用），头部互联网企业（谷歌、阿里云、腾讯云）已逐步支持，需服务器 / CDN 开启 QUIC 协议。

### 总结

1. HTTP/1.1 解决了基础的连接复用和虚拟主机问题，但存在严重的队头阻塞；
2. HTTP/2 基于 TCP 优化应用层，解决了应用层队头阻塞，但未解决 TCP 层的核心问题；
3. HTTP/3 切换到 UDP 协议，彻底解决队头阻塞，降低连接延迟，适配移动网络场景，是未来的主流方向。`;export{T as default};
