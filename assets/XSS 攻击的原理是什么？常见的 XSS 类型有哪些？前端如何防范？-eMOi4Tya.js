const n='## 1. XSS 攻击的原理是什么？常见的 XSS 类型有哪些？前端如何防范？\n\n### 问题核心\n\nXSS 是前端最常见的安全漏洞之一，考察对跨站脚本攻击的本质理解、类型区分及落地防范手段。\n\n### 参考答案\n\n#### （1）攻击原理\n\nXSS（跨站脚本攻击，Cross-Site Scripting）：攻击者将恶意脚本（JS、HTML 等）注入到网页中，当用户访问该页面时，恶意脚本会被浏览器执行，从而窃取用户数据（Cookie、Token）、伪造操作、劫持会话等。核心是**绕过前端过滤 / 后端校验，让恶意代码被正常页面解析执行**。\n\n#### （2）常见类型\n\n#### （3）前端防范手段\n\n1. **输入 / 输出转义**：对用户输入的所有内容（表单、URL 参数）进行 HTML 转义（如 `<` → `&lt;`、`>` → `&gt;`），避免直接用 `innerHTML`/`document.write` 渲染；优先用 `textContent` 渲染文本。\n2. **启用 CSP 策略**：限制脚本加载源、禁止内联脚本 /`eval` 执行，从根源阻止恶意脚本执行。\n3. **Cookie 保护**：敏感 Cookie 标记为 `HttpOnly`（禁止 JS 读取）、`Secure`（仅 HTTPS 传输）。\n4. **避免危险 API**：禁用 `eval`、`setTimeout("字符串")`、`Function` 构造函数等动态执行代码的 API。\n5. **前端校验辅助**：对输入内容做长度、格式限制（如过滤 `<script>` 标签），但仅作为辅助（核心靠后端）。';export{n as default};
