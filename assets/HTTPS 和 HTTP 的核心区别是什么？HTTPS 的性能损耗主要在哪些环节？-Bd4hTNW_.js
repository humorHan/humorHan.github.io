const _=`### 12. HTTPS 和 HTTP 的核心区别是什么？HTTPS 的性能损耗主要在哪些环节？

#### 核心区别

#### 性能损耗环节

1. **TLS 握手阶段**：首次握手需 2-3 次 TCP 交互（TCP 三次握手 + TLS 握手），消耗 CPU 资源验证证书；
2. **加解密环节**：数据传输时的对称加密（AES）、非对称加密（RSA/ECC）操作，消耗服务器 / 客户端 CPU；
3. **证书验证**：客户端验证证书合法性（链式验证、吊销检查），增加耗时；
4. **缓存优化**：HTTPS 缓存策略更严格（如部分代理不缓存 HTTPS 内容）。

**优化方式**：会话复用、OCSP Stapling、HTTP2、TLS1.3、CDN 加速。`;export{_ as default};
