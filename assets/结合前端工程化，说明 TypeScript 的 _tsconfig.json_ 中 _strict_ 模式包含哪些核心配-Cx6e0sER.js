const n='#### 结合前端工程化，说明 TypeScript 的 `tsconfig.json` 中 `strict` 模式包含哪些核心配置？开启 `strict` 模式对项目有哪些好处和潜在问题？\n\n**答案**：`strict` 是 `tsconfig.json` 的总开关，开启后会启用以下核心严格检查配置：\n\n1. `strictNullChecks`：严格空检查（`null`/`undefined` 不能赋值给非空类型）；\n2. `strictFunctionTypes`：严格函数类型检查（禁止函数参数双向协变）；\n3. `strictPropertyInitialization`：类属性必须初始化（或明确标记可选）；\n4. `noImplicitAny`：禁止隐式 `any` 类型；\n5. `noImplicitThis`：禁止 `this` 隐式为 `any` 类型；\n6. `alwaysStrict`：以严格模式解析代码并输出 `"use strict"`。\n\n**好处**：\n\n- 提前发现类型错误（如空指针、类型不匹配），减少时 Bug；\n- 强制规范代码，提升代码可维护性和类型安全性；\n- 符合大厂代码规范，降低团队协作成本。\n\n**潜在问题**：\n\n- 兼容老 JS 项目时成本高（需大量补全类型、处理空值）；\n- 初期开发效率降低（需编写更多类型声明）；\n- 第三方库无类型声明时，需手动编写 `.d.ts` 声明文件。\n\n---';export{n as default};
