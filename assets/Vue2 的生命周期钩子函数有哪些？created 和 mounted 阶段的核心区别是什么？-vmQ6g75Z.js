const e="### 2. Vue2 的生命周期钩子函数有哪些？created 和 mounted 阶段的核心区别是什么？\n\n**答：**\n\n- **生命周期钩子函数（完整流程）**：初始化阶段：`beforeCreate` → `created`；挂载阶段：`beforeMount` → `mounted`；更新阶段：`beforeUpdate` → `updated`；销毁阶段：`beforeDestroy` → `destroyed`；特殊钩子：`activated`（keep-alive 激活）、`deactivated`（keep-alive 失活）、`errorCaptured`（错误捕获）。\n- **created 和 mounted 的核心区别**：\n\n  1. 执行时机：`created` 在实例创建完成后执行（数据观测、事件初始化完成，但 DOM 未挂载）；`mounted` 在 DOM 挂载完成后执行（el 被替换为真实 DOM，可访问 DOM 元素）；\n  2. 可操作内容：`created` 中可访问 data、methods，但无法操作 DOM（如 `document.getElementById` 为 null）；`mounted` 中可正常操作 DOM，且子组件已完成挂载；\n  3. 应用场景：`created` 适合初始化数据、调用异步接口（无需等待 DOM）；`mounted` 适合操作 DOM、初始化第三方插件（如 ECharts 渲染）。";export{e as default};
