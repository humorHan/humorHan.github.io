const _=`### CSS 预处理器（Sass/LESS/Stylus）的核心区别？使用预处理器的价值？

**答案**：

#### 核心区别：

#### 使用预处理器的价值：

1. 变量：统一管理样式值（如主题色、字体大小）；
2. 嵌套：简化选择器书写（如 \`.box .title\` → \`.box { .title {} }\`）；
3. 混合（Mixin）：复用样式片段（如 clearfix、三角形）；
4. 继承：减少重复样式（如多个按钮继承基础样式）；
5. 运算：动态计算尺寸（如 \`width: 100px - 20px\`）；
6. 模块化：拆分样式文件（如_reset.scss、_common.scss），便于维护；
7. 函数：处理颜色 / 尺寸（如 \`darken($color, 10%)\` 加深颜色）。`;export{_ as default};
