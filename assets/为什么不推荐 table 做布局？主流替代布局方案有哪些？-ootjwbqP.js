const _=`### 为什么不推荐 table 做布局？主流替代布局方案有哪些？

**答案**：

#### 不推荐 table 布局的原因：

- 结构与样式耦合（需通过 table 标签嵌套实现布局，代码冗余）；
- 可维护性差（修改布局需调整大量 table/tr/td 标签）；
- 响应式适配困难（table 宽度固定，难以适配移动端）；
- SEO 不友好（搜索引擎难以解析非表格数据的 table 布局）；
- 加载速度慢（table 需加载完所有内容才渲染，阻塞页面显示）。

#### 主流替代方案：

1. Flex 布局：一维布局（行 / 列），适配大部分场景（如导航、卡片布局）；
2. Grid 布局：二维布局（行 + 列），适合复杂网格布局（如仪表盘、电商商品列表）；
3. 浮动布局（float）：兼容旧浏览器，现已逐步被 Flex/Grid 替代；
4. 定位布局（position）：适用于局部精准定位（如弹窗、悬浮按钮）；
5. 弹性盒 + 媒体查询：实现响应式布局（适配不同屏幕尺寸）。`;export{_ as default};
