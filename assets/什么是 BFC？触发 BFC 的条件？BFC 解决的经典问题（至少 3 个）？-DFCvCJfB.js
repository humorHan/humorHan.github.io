const n="### 什么是 BFC？触发 BFC 的条件？BFC 解决的经典问题（至少 3 个）？\n\n**答案**：BFC（Block Formatting Context，块级格式化上下文）是 CSS 的一种渲染规则，BFC 内部的元素布局不受外部影响，外部也不影响内部，且 BFC 区域不会与浮动元素重叠。\n\n#### 触发 BFC 的条件（满足其一即可）：\n\n1. 根元素（`<html>`）；\n2. `overflow` 值不为 `visible`（如 `hidden`/`auto`/`scroll`）；\n3. `float` 值不为 `none`（`left`/`right`）；\n4. `position` 值为 `absolute`/`fixed`/`sticky`；\n5. `display` 值为 `flex`/`grid`/`inline-block`/`table-cell`/`table-caption`；\n6. `contain: layout`/`content`/`paint`。\n\n#### BFC 解决的经典问题：\n\n1. 清除浮动（父元素高度塌陷：子元素浮动后，父元素高度为 0，给父元素触发 BFC 即可）；\n2. 防止 margin 重叠（两个相邻 BFC 元素的 margin 不会重叠）；\n3. 防止元素被浮动元素覆盖（如文字环绕图片时，给文字容器触发 BFC，避免文字被图片覆盖）；\n4. 自适应两栏布局（左侧浮动，右侧触发 BFC，自动填充剩余宽度）。";export{n as default};
