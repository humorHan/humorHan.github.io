const _=`### 什么是活动对象（AO）？与变量对象（VO）的区别？仅存在于哪个执行上下文中？

**答：**

#### （1）活动对象（AO）定义

活动对象（Activation Object）是「执行阶段的变量对象（VO）」，此时变量对象被「激活」，可被代码访问，是函数执行上下文特有的概念。

#### （2）与变量对象（VO）的核心区别

#### （3）仅存在于函数执行上下文中

- 全局执行上下文的变量对象是「全局对象（window）」，不会转为 AO（但全局对象本身可访问）；
- 函数执行上下文在执行阶段，VO 激活为 AO，包含 \`arguments\` 对象（函数特有），执行完毕后 AO 被销毁。`;export{_ as default};
