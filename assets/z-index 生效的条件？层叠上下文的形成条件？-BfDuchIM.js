const n="### z-index 生效的条件？层叠上下文的形成条件？\n\n**答案**：\n\n#### z-index 生效条件：\n\nz-index 用于控制元素的层叠顺序，仅在以下条件下生效：\n\n1. 元素的 `position` 属性不为 `static`（即 `relative`/`absolute`/`fixed`/`sticky`）；\n2. 元素属于某个层叠上下文（否则 z-index 相对父层叠上下文生效）。\n\n#### 层叠上下文的形成条件（满足其一即可）：\n\n1. 根元素（`<html>`）；\n2. `position` 不为 `static` 且 `z-index` 不为 `auto`；\n3. `display` 为 `flex/grid` 且 `z-index` 不为 `auto`；\n4. `opacity` < 1；\n5. `transform` 不为 `none`；\n6. `filter` 不为 `none`；\n7. `will-change` 为 `transform`/`opacity` 等；\n8. `mix-blend-mode` 不为 `normal`；\n9. `isolation: isolate`；\n10. `position: fixed`（部分浏览器）。\n\n#### 层叠顺序（同一层叠上下文内，从下到上）：\n\n`background/border` → 负 z-index → 文档流块级元素 → 文档流行内元素 → float 元素 → z-index:auto/0 → 正 z-index。";export{n as default};
