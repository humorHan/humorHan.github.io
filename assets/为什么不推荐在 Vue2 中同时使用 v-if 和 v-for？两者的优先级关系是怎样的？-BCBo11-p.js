const _='### 3. 为什么不推荐在 Vue2 中同时使用 v-if 和 v-for？两者的优先级关系是怎样的？\n\n**答：**\n\n- **优先级关系**：Vue2 中 `v-for` 的优先级高于 `v-if`（Vue3 中相反）。\n- **不推荐同时使用的原因**：\n\n  1. 性能问题：由于 `v-for` 优先级更高，会先遍历所有元素，再对每个元素执行 `v-if` 判断，即使只有少量元素需要渲染，也会遍历全部数据，造成不必要的性能开销；\n  2. 逻辑混乱：同时使用时，`v-if` 无法直接访问 `v-for` 的循环变量（如 `v-if="item.flag"` 虽可行，但逻辑耦合），易引发代码可读性问题。\n- **优化方案**：\n\n  1. 将数据提前过滤（如通过 computed 筛选需要渲染的数组）；\n  2. 将 `v-if` 移到 `v-for` 的父元素上，先判断是否渲染整个列表，再执行循环。';export{_ as default};
