const n=`### CSS3 新增核心特性？至少列举 8 个并说明用途。

**答案**：CSS3 新增核心特性及用途：

1. **选择器增强**：新增 \`::nth-child()\`/\`:not()\`/\`[attr^="value"]\` 等选择器，更精准选中元素；
2. **圆角（border-radius）**：实现圆角元素，替代图片圆角；
3. **阴影（box-shadow/text-shadow）**：给盒子 / 文字添加阴影，提升视觉层次；
4. **渐变（linear-gradient/radial-gradient/conic-gradient）**：实现线性 / 径向 / 锥形渐变背景，替代渐变图片；
5. **过渡（transition）**：实现元素属性的平滑过渡（如 hover 效果）；
6. **动画（animation）**：实现复杂的关键帧动画（无需 JS）；
7. **变形（transform）**：实现元素旋转（rotate）、缩放（scale）、平移（translate）、倾斜（skew），无布局偏移；
8. **多列布局（column-count/column-gap）**：实现报纸式多列文本布局；
9. **弹性盒（Flex）**：一维布局方案，简化居中 / 等分布局；
10. **网格（Grid）**：二维布局方案，适配复杂网格；
11. **CSS 变量（--var）**：自定义属性，统一管理样式值（如主题色）；
12. **媒体查询（@media）**：实现响应式布局，适配不同屏幕；
13. **滤镜（filter）**：实现图片模糊、灰度、亮度调整等效果；
14. **伪元素增强（::before/::after）**：支持 content 动态生成内容，实现装饰性元素。`;export{n as default};
