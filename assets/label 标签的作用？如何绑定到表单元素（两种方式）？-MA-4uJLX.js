const e='### label 标签的作用？如何绑定到表单元素（两种方式）？\n\n**答案**：label 标签的核心作用：① 扩大表单元素的点击区域（点击 label 即点击对应表单元素，提升体验）；② 增强无障碍访问（屏幕阅读器可关联 label 和表单元素）。\n\n#### 绑定方式：\n\n1. **嵌套式**：将表单元素嵌套在 label 标签内：\n\n```html\n<label> 用户名：<input type="text" name="username" /></label>\n```\n\n1. **for-id 绑定**：label 的 `for` 属性值等于表单元素的 `id` 值：\n\n```html\n<label for="username">用户名：</label><input type="text" id="username" name="username" />\n```';export{e as default};
