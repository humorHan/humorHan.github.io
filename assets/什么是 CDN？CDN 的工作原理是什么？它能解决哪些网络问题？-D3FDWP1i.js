const _=`## 8. 什么是 CDN？CDN 的工作原理是什么？它能解决哪些网络问题？

### CDN 定义

内容分发网络（Content Delivery Network）：将静态资源（JS/CSS/ 图片 / 视频）部署到全球 / 全国的边缘节点服务器，用户就近获取资源。

### 工作原理

1. **用户请求**：用户访问含 CDN 资源的页面，发起资源请求。
2. **DNS 解析**：DNS 将 CDN 域名解析到「就近的边缘节点 IP」（通过 GSLB 全局负载均衡）。
3. **边缘节点响应**：

   - 节点有缓存：直接返回资源给用户；
   - 节点无缓存：回源站（资源主服务器）获取资源，缓存到节点后再返回。
4. **缓存更新**：边缘节点按 TTL 规则定期更新缓存，保证资源最新。

### 解决的网络问题

1. 降低访问延迟（就近取资源，减少网络链路）；
2. 缓解源站压力（大部分请求由边缘节点处理）；
3. 提高可用性（节点故障自动切换到其他节点）；
4. 抗 DDoS 攻击（分散流量，避免源站被集中攻击）；
5. 解决跨地域访问慢的问题（跨境 / 跨运营商优化）。`;export{_ as default};
