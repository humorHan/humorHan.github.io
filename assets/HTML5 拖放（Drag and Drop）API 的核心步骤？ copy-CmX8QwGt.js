const a="### HTML5 拖放（Drag and Drop）API 的核心步骤？\n\n**答案**：HTML5 拖放 API 实现元素拖拽需以下核心步骤：\n\n1. **设置可拖拽**：给拖拽元素添加 `draggable=\"true\"`（图片 / 链接默认可拖拽）；\n2. **监听拖拽事件（拖拽元素）**：\n   - `dragstart`：开始拖拽时触发（可设置拖拽数据 `event.dataTransfer.setData('text', 元素ID)`）；\n   - `drag`：拖拽过程中持续触发（可做样式变化）；\n   - `dragend`：拖拽结束时触发（重置样式）；\n\n3. **监听放置事件（目标元素）**：\n   - `dragover`：拖拽元素经过目标时触发（需阻止默认行为 `event.preventDefault()`，否则无法放置）；\n   - `dragenter`：拖拽元素进入目标时触发（修改目标样式）；\n   - `dragleave`：拖拽元素离开目标时触发（重置目标样式）；\n   - `drop`：拖拽元素放置到目标时触发（获取拖拽数据 `event.dataTransfer.getData('text')`，执行放置逻辑）。";export{a as default};
