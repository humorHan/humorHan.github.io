const n="### 28. CSS 中的 @ 规则有哪些？（@import/@media/@keyframes 等）各自作用？\n\n**答案**：CSS 的 @ 规则（At-rule）是以 `@` 开头的特殊语法，用于定义 CSS 的特殊规则 / 行为，核心 @ 规则及作用：\n\n1. `@import`：引入外部 CSS 文件；\n2. css\n\n```css\n@import url(\"style.css\"); /* 引入外部样式 */@import url(\"mobile.css\") screen and (max-width: 768px); /* 条件引入 */\n```\n\n1. 注意：`@import` 需写在 CSS 文件顶部，且会阻塞渲染，优先使用 `<link>` 替代。\n2. `@media`：媒体查询，根据设备特性（宽度 / 分辨率 / 方向）应用不同样式；\n3. css\n\n```css\n@media screen and (max-width: 768px) { .box { padding: 10px; } }\n```\n\n1. `@keyframes`：定义 CSS 动画的关键帧；\n2. css\n\n```css\n@keyframes move { 0% { transform: translateX(0); } 100% { transform: translateX(100px); } }\n```\n\n1. `@font-face`：自定义字体（如 iconfont / 思源字体）；\n2. css\n\n```css\n@font-face {font-family: 'MyFont';src: url('myfont.woff2') format('woff2'), url('myfont.woff') format('woff');}\n```\n\n1. `@supports`：特性检测，判断浏览器是否支持某 CSS 属性；\n2. css\n\n```css\n@supports (display: grid) { .box { display: grid; } }@supports not (display: grid) { .box { display: flex; } }\n```\n\n1. `@page`：定义打印页面的样式（如页边距 / 页码）；\n2. css\n\n```css\n@page { margin: 2cm; }@page :first { margin-top: 5cm; } /* 第一页特殊样式 */\n```\n\n1. `@charset`：声明 CSS 文件的字符编码（需写在第一行）；\n2. css\n\n```css\n@charset \"UTF-8\";\n```\n\n1. `@namespace`：定义 XML 命名空间，用于处理 XML/HTML 混合文档的样式；\n2. css\n\n```css\n@namespace svg url(\"http://www.w3.org/2000/svg\");svg|rect { fill: red; } /* 仅匹配SVG的rect元素 */\n```\n\n1. `@layer`：CSS 层叠层，定义样式的优先级层级（解决样式覆盖问题）；\n2. css\n\n```css\n@layer base, component, override; /* 定义层级，override优先级最高 */@layer component { .box { color: blue; } }@layer override { .box { color: red; } } /* 最终生效 */\n```";export{n as default};
