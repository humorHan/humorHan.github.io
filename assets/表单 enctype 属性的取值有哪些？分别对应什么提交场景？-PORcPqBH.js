const _='### 表单 enctype 属性的取值有哪些？分别对应什么提交场景？\n\n**答案**：enctype 用于指定表单数据提交的编码类型，仅在 `method="post"` 时生效，取值及场景：\n\n1. `application/x-www-form-urlencoded`（默认）：\n   - 编码规则：将表单数据转为 `key1=value1&key2=value2` 格式，特殊字符转义；\n   - 场景：普通表单提交（如登录、注册，无文件上传）。\n\n2. `multipart/form-data`：\n   - 编码规则：以二进制分块方式传输数据，不转义字符；\n   - 场景：包含文件上传的表单（如 `<input type="file">`）。\n\n3. `text/plain`：\n   - 编码规则：纯文本格式，空格转义为 `+`，但特殊字符不转义；\n   - 场景：仅用于调试，极少在生产环境使用。';export{_ as default};
