const n='### CSS 媒体查询（Media Query）的语法？常用查询条件（宽度 / 分辨率 / 方向）？\n\n**答案**：\n\n#### 语法：\n\ncss\n\n```css\n/* 基础语法 */@media [not/only] 媒体类型 [and 媒体特性] {/* 样式规则 */}/* 引入外部样式 */\n<link rel="stylesheet" media="媒体查询条件" href="style.css">\n```\n\n#### 媒体类型：\n\n- `all`：所有设备（默认）；\n- `screen`：屏幕设备（电脑 / 手机 / 平板）；\n- `print`：打印设备；\n- `speech`：屏幕阅读器。\n\n#### 常用媒体特性（查询条件）：\n\n1. **宽度 / 高度**：\n\n   - `width`/`min-width`/`max-width`：视口宽度（如 `max-width: 768px`）；\n   - `height`/`min-height`/`max-height`：视口高度；\n   - `device-width`/`device-height`：设备物理宽度 / 高度。\n2. **分辨率**：\n\n   - `resolution`：屏幕分辨率（如 `resolution: 2dppx` 适配 Retina 屏）；\n   - `min-resolution`/`max-resolution`。\n3. **方向**：\n\n   - `orientation: portrait`：竖屏（高度 ≥ 宽度）；\n   - `orientation: landscape`：横屏（宽度 > 高度）。\n4. **其他**：\n\n   - `aspect-ratio`：视口宽高比；\n   - `color`：设备颜色位数；\n   - `hover`：设备是否支持鼠标悬浮（如移动端 / PC 端区分）。\n\n#### 示例：\n\ncss\n\n```css\n/* 竖屏且宽度≤768px时生效 */@media screen and (orientation: portrait) and (max-width: 768px) {.container { padding: 10px; }}\n```';export{n as default};
