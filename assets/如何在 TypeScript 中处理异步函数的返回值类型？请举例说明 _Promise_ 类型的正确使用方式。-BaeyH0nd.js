const e="#### 如何在 TypeScript 中处理异步函数的返回值类型？请举例说明 `Promise` 类型的正确使用方式。\n\n**答案**：异步函数（`async` 修饰）的返回值默认是 `Promise<T>` 类型（T 为函数返回值的类型），处理方式：\n\n1. 显式声明异步函数的返回值类型；\n2. 通过 `await`/`.then()` 提取 Promise 内部类型；\n3. 处理错误类型（`Promise<SuccessType | Error>`）。\n\n**示例**：\n\n```typescript\n// 1. 声明异步函数返回值类型async function fetchUser(): Promise<{ id: number; name: string }> {const res = await fetch('/api/user');return res.json();}// 2. 消费 Promise 类型async function useUser() {// 方式1：await 提取类型const user = await fetchUser(); \n  console.log(user.id); // user 类型为 { id: number; name: string }// 方式2：.then 提取类型fetchUser().then(user => console.log(user.name));}\n```";export{e as default};
