const _=`## 12. 网络劫持的常见类型有哪些？前端如何防范网络劫持？

### 常见劫持类型

1. **DNS 劫持**：篡改 DNS 解析结果，将域名解析到恶意 IP（如跳转到钓鱼网站、广告页面）；
2. **HTTP 劫持**：监听 HTTP 明文传输数据，篡改响应内容（如插入广告脚本、篡改页面内容）；
3. **TCP 劫持**：伪造 TCP 数据包，冒充通信双方发送数据，窃取 / 篡改信息；
4. **SSL 中间人攻击**：伪造 HTTPS 证书，劫持加密通信（需用户信任伪造证书）。

### 前端防范手段

1. **强制 HTTPS**：使用 HTTPS 加密传输，防止 HTTP 明文劫持；配置 HSTS（\`Strict-Transport-Security\`），强制浏览器使用 HTTPS，避免降级攻击；
2. **DNS 安全**：使用 DNSSEC（DNS 加密）、HTTPDNS（绕过运营商 DNS），防止 DNS 劫持；
3. **资源完整性校验**：对静态资源添加 \`integrity\` 属性（如 \`<script src="xxx.js" integrity="sha256-xxx">\`），校验资源哈希，篡改则不执行；
4. **监控异常**：前端检测页面是否被插入非预期脚本 / 内容，发现后告警 / 清理；
5. **CDN 加速**：使用高防 CDN，减少劫持概率（CDN 节点有反劫持机制）；
6. **证书校验**：前端可校验服务端证书（避免中间人攻击，如小程序 / APP 端）；
7. **敏感数据加密**：对核心数据（如支付信息）额外加密，即使被劫持也无法解密。`;export{_ as default};
