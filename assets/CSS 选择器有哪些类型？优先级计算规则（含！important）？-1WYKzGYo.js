const n='### CSS 选择器有哪些类型？优先级计算规则（含！important）？\n\n**答案**：\n\n#### CSS 选择器类型：\n\n1. 基础选择器：通配符（`*`）、元素选择器（`div`）、类选择器（`.class`）、ID 选择器（`#id`）、属性选择器（`[type="text"]`）；\n2. 组合选择器：后代选择器（`div p`）、子选择器（`div > p`）、相邻兄弟选择器（`div + p`）、通用兄弟选择器（`div ~ p`）；\n3. 伪类选择器：状态伪类（`:hover`/`:active`/`:focus`）、结构伪类（`:nth-child()`/`:first-child`）、表单伪类（`:checked`/`:disabled`）、否定伪类（`:not()`）；\n4. 伪元素选择器：`::before`/`::after`/`::first-letter`/`::first-line`。\n\n#### 优先级计算规则（从高到低）：\n\n1. `!important`：优先级最高（需注意：`!important` 写在属性值后，如 `color: red !important;`）；\n2. 内联样式（`style="color: red"`）：权重 1000；\n3. ID 选择器：权重 100；\n4. 类选择器 / 属性选择器 / 伪类选择器：权重 10；\n5. 元素选择器 / 伪元素选择器：权重 1；\n6. 通配符（`*`）/ 继承样式：权重 0。\n\n#### 计算规则：\n\n- 权重按 “千 / 百 / 十 / 个” 位累加（如 `#id .class div` 权重 = 100+10+1=111）；\n- 权重相同则后写的样式覆盖先写的；\n- `!important` 仅提升属性优先级，不影响选择器；继承的样式加 `!important` 仍低于直接选中的样式。';export{n as default};
